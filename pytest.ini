[pytest]
# Bare `pytest` collects only the package tests; ci/wasm is opt-in via an
# explicit path (its Playwright deps are not part of the test environments).
testpaths = quantecon
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
